diff --git a/.env.example b/.env.example
index 899a681..0e55375 100644
--- a/.env.example
+++ b/.env.example
@@ -104,10 +104,16 @@ EVPOLY_MM_SPORT_QUOTE_SIZE_MULT=1.2
 EVPOLY_MM_SPORT_PAIR_BASELINE_QUOTE_SIZE_MULT=1.2
 EVPOLY_MM_SPORT_MULTIPLE_COLLATERAL_CAP_MULT=0.45
 EVPOLY_MM_SPORT_DEPTH_RATIO_COLLATERAL_CAP_MULT=0.90
+EVPOLY_MM_SPORT_NONSPORT_QUOTE_SIZE_MODE=
+EVPOLY_MM_SPORT_NONSPORT_QUOTE_SIZE_MULT=
+EVPOLY_MM_SPORT_NONSPORT_MULTIPLE_COLLATERAL_CAP_MULT=
+EVPOLY_MM_SPORT_NONSPORT_DEPTH_RATIO_COLLATERAL_CAP_MULT=
 EVPOLY_MM_SPORT_POST_ONLY=true
 EVPOLY_MM_SPORT_ORDER_SUBMIT_TIMEOUT_MS=10000
 EVPOLY_MM_SPORT_MAX_SHARE_RATIO=0.05
 EVPOLY_MM_SPORT_MIN_TOP_DEPTH_USD=1100
+EVPOLY_MM_SPORT_NONSPORT_MAX_SHARE_RATIO=
+EVPOLY_MM_SPORT_NONSPORT_MIN_TOP_DEPTH_USD=
 EVPOLY_MM_SPORT_MIN_ENTRY_TOP_BID_PRICE=0.10
 EVPOLY_MM_SPORT_ALLOWED_SPORT_LEAGUE_CODES=
 EVPOLY_MM_SPORT_BLOCKED_SPORT_LEAGUE_CODES=
diff --git a/.env.full.example b/.env.full.example
index ddd91ce..d537f9e 100644
--- a/.env.full.example
+++ b/.env.full.example
@@ -1093,10 +1093,16 @@ EVPOLY_MM_SPORT_QUOTE_SIZE_MULT=1.2
 EVPOLY_MM_SPORT_PAIR_BASELINE_QUOTE_SIZE_MULT=1.2
 EVPOLY_MM_SPORT_MULTIPLE_COLLATERAL_CAP_MULT=0.45
 EVPOLY_MM_SPORT_DEPTH_RATIO_COLLATERAL_CAP_MULT=0.90
+EVPOLY_MM_SPORT_NONSPORT_QUOTE_SIZE_MODE=
+EVPOLY_MM_SPORT_NONSPORT_QUOTE_SIZE_MULT=
+EVPOLY_MM_SPORT_NONSPORT_MULTIPLE_COLLATERAL_CAP_MULT=
+EVPOLY_MM_SPORT_NONSPORT_DEPTH_RATIO_COLLATERAL_CAP_MULT=
 EVPOLY_MM_SPORT_POST_ONLY=true
 EVPOLY_MM_SPORT_ORDER_SUBMIT_TIMEOUT_MS=10000
 EVPOLY_MM_SPORT_MAX_SHARE_RATIO=0.05
 EVPOLY_MM_SPORT_MIN_TOP_DEPTH_USD=1100
+EVPOLY_MM_SPORT_NONSPORT_MAX_SHARE_RATIO=
+EVPOLY_MM_SPORT_NONSPORT_MIN_TOP_DEPTH_USD=
 EVPOLY_MM_SPORT_MIN_ENTRY_TOP_BID_PRICE=0.10
 EVPOLY_MM_SPORT_ALLOWED_SPORT_LEAGUE_CODES=
 EVPOLY_MM_SPORT_BLOCKED_SPORT_LEAGUE_CODES=
diff --git a/docs/mm_sport_v1.md b/docs/mm_sport_v1.md
index b456cc5..a7cd141 100644
--- a/docs/mm_sport_v1.md
+++ b/docs/mm_sport_v1.md
@@ -59,6 +59,11 @@ Sports route applies match-only, pregame-only, league filters, and live-game gua
   - `EVPOLY_MM_SPORT_ALLOW_SPONSORED_REWARDS=true`
   - sponsored rewards are allowed by default
   - when set to `false`, markets are excluded only if sponsored rewards are at least `EVPOLY_MM_SPORT_SPONSORED_REWARD_MIN_SHARE=0.50` of total daily rewards
+- Route-specific BUY sizing:
+  - existing `EVPOLY_MM_SPORT_*` sizing keys remain the Sport/default profile
+  - optional `EVPOLY_MM_SPORT_NONSPORT_*` overrides apply to Non-S markets in Non-S or Dual route
+  - missing Non-S overrides fall back to Sport/default values
+  - overrides affect fresh BUY entry sizing and BUY-side depth/ratio hygiene only; SELL exits and inventory cleanup keep existing logic
 - Live-game guard:
   - `EVPOLY_MM_SPORT_POLYMARKET_LIVE_GUARD_ENABLE=true`
   - `EVPOLY_MM_SPORT_POLYMARKET_LIVE_GUARD_WS_ENABLE=true`
@@ -84,6 +89,12 @@ Sports route applies match-only, pregame-only, league filters, and live-game gua
 - `EVPOLY_MM_SPORT_MULTIPLE_COLLATERAL_CAP_MULT`
 - `EVPOLY_MM_SPORT_DEPTH_RATIO_COLLATERAL_CAP_MULT`
 - `EVPOLY_MM_SPORT_MIN_TOP_DEPTH_USD`
+- `EVPOLY_MM_SPORT_NONSPORT_QUOTE_SIZE_MODE`
+- `EVPOLY_MM_SPORT_NONSPORT_QUOTE_SIZE_MULT`
+- `EVPOLY_MM_SPORT_NONSPORT_MULTIPLE_COLLATERAL_CAP_MULT`
+- `EVPOLY_MM_SPORT_NONSPORT_DEPTH_RATIO_COLLATERAL_CAP_MULT`
+- `EVPOLY_MM_SPORT_NONSPORT_MIN_TOP_DEPTH_USD`
+- `EVPOLY_MM_SPORT_NONSPORT_MAX_SHARE_RATIO`
 - `EVPOLY_MM_SPORT_MIN_ENTRY_TOP_BID_PRICE`
 - `EVPOLY_MM_SPORT_NONSPORT_END_EXIT_START_SEC`
 - `EVPOLY_MM_SPORT_ALLOW_SPONSORED_REWARDS`
diff --git a/src/main.rs b/src/main.rs
index 4fc09c4..44d25da 100644
--- a/src/main.rs
+++ b/src/main.rs
@@ -17412,7 +17412,15 @@ async fn main() -> Result<()> {
                             .copied()
                             .filter(|value| value.is_finite() && *value > 0.0)
                             .unwrap_or(0.01);
-                        let ratio_limit = mm_sport_cfg_for_loop.max_share_ratio.clamp(0.01, 0.99);
+                        let ratio_limit = mm_sport_cfg_for_loop
+                            .sizing_for_market(
+                                market_by_condition
+                                    .get(condition_id)
+                                    .map(|market| market.is_sports_market)
+                                    .unwrap_or(true),
+                            )
+                            .max_share_ratio
+                            .clamp(0.01, 0.99);
                         let mut pair_min_top_depth_usd = f64::INFINITY;
                         let mut visible_ratio_breached = false;
                         let mut front_ratio_breached = false;
@@ -17773,7 +17781,8 @@ async fn main() -> Result<()> {
                     last_watchdog_pause_reason_by_condition = watchdog_pause_state_this_pass;
 
                     if mm_sport_cfg_for_loop.event_driven_enable && api_for_mm_sport.ws_enabled() {
-                        let ratio_limit = mm_sport_cfg_for_loop.max_share_ratio.clamp(0.01, 0.99);
+                        let report_ratio_limit =
+                            mm_sport_cfg_for_loop.max_share_ratio.clamp(0.01, 0.99);
                         let fifo_ws_gap_cancel_ms =
                             mm_sport_cfg_for_loop.fifo_ws_gap_cancel_ms.max(500);
                         let mut fifo_report_condition_count = 0usize;
@@ -17798,6 +17807,13 @@ async fn main() -> Result<()> {
                             if rows.is_empty() {
                                 continue;
                             }
+                            let condition_sizing = mm_sport_cfg_for_loop.sizing_for_market(
+                                market_by_condition
+                                    .get(condition_id)
+                                    .map(|market| market.is_sports_market)
+                                    .unwrap_or(true),
+                            );
+                            let ratio_limit = condition_sizing.max_share_ratio.clamp(0.01, 0.99);
                             let tick_size = market_by_condition
                                 .get(condition_id)
                                 .map(|market| market.minimum_tick_size)
@@ -18102,7 +18118,7 @@ async fn main() -> Result<()> {
                                 json!({
                                     "strategy_id": STRATEGY_ID_MM_SPORT_V1,
                                     "condition_id": condition_id,
-                                    "quote_size_mode": mm_sport_cfg_for_loop.quote_size_mode.as_str(),
+                                    "quote_size_mode": condition_sizing.quote_size_mode.as_str(),
                                     "max_share_ratio": ratio_limit,
                                     "fifo_ws_gap_cancel_ms": fifo_ws_gap_cancel_ms,
                                     "ratio_pause_sec": mm_sport_cfg_for_loop.ratio_pause_sec,
@@ -18162,7 +18178,7 @@ async fn main() -> Result<()> {
                                 "front_ratio_missing_orders": fifo_report_front_ratio_missing,
                                 "max_front_ratio": fifo_report_max_front_ratio,
                                 "min_ahead_worst": fifo_report_min_ahead_worst,
-                                "max_share_ratio": ratio_limit,
+                                "max_share_ratio": report_ratio_limit,
                                 "fifo_ws_gap_cancel_ms": fifo_ws_gap_cancel_ms,
                                 "market_ws_age_ms": market_ws_age_ms,
                                 "max_orderbook_ws_age_ms": fifo_report_max_ws_age_ms,
@@ -18328,40 +18344,42 @@ async fn main() -> Result<()> {
                     });
                     ratio_recovery_streak_by_condition
                         .retain(|condition_id, _| ratio_blocked_conditions.contains(condition_id));
-                    let quote_notional_cap_usd = match mm_sport_cfg_for_loop.quote_size_mode {
-                        mm::MmSportQuoteSizeMode::DepthRatio => {
-                            mm_sport_refresh_available_collateral_cap_usd(
-                                &api_for_mm_sport,
-                                now_ms,
-                                &mut mm_sport_available_collateral_cached,
-                                &mut mm_sport_available_collateral_cached_at_ms,
-                                &mut mm_sport_balance_error_log_ms,
-                                &mut mm_sport_allowance_zero_fallback_log_ms,
-                                "depth_ratio",
-                                mm_sport_cfg_for_loop.depth_ratio_collateral_cap_mult,
-                            )
-                            .await
-                        }
-                        mm::MmSportQuoteSizeMode::Multiple => {
-                            mm_sport_refresh_available_collateral_cap_usd(
-                                &api_for_mm_sport,
-                                now_ms,
-                                &mut mm_sport_available_collateral_cached,
-                                &mut mm_sport_available_collateral_cached_at_ms,
-                                &mut mm_sport_balance_error_log_ms,
-                                &mut mm_sport_allowance_zero_fallback_log_ms,
-                                "multiple",
-                                mm_sport_cfg_for_loop.multiple_collateral_cap_mult,
-                            )
-                            .await
-                        }
-                    };
                     let mut depth_skip_active_this_pass: std::collections::HashSet<String> =
                         std::collections::HashSet::new();
                     let mut ratio_infeasible_active_this_pass: std::collections::HashSet<String> =
                         std::collections::HashSet::new();
 
                     for market in &active_markets {
+                        let market_sizing =
+                            mm_sport_cfg_for_loop.sizing_for_market(market.is_sports_market);
+                        let quote_notional_cap_usd = match market_sizing.quote_size_mode {
+                            mm::MmSportQuoteSizeMode::DepthRatio => {
+                                mm_sport_refresh_available_collateral_cap_usd(
+                                    &api_for_mm_sport,
+                                    now_ms,
+                                    &mut mm_sport_available_collateral_cached,
+                                    &mut mm_sport_available_collateral_cached_at_ms,
+                                    &mut mm_sport_balance_error_log_ms,
+                                    &mut mm_sport_allowance_zero_fallback_log_ms,
+                                    "depth_ratio",
+                                    market_sizing.depth_ratio_collateral_cap_mult,
+                                )
+                                .await
+                            }
+                            mm::MmSportQuoteSizeMode::Multiple => {
+                                mm_sport_refresh_available_collateral_cap_usd(
+                                    &api_for_mm_sport,
+                                    now_ms,
+                                    &mut mm_sport_available_collateral_cached,
+                                    &mut mm_sport_available_collateral_cached_at_ms,
+                                    &mut mm_sport_balance_error_log_ms,
+                                    &mut mm_sport_allowance_zero_fallback_log_ms,
+                                    "multiple",
+                                    market_sizing.multiple_collateral_cap_mult,
+                                )
+                                .await
+                            }
+                        };
                         let condition_key_lc = market.condition_id.trim().to_ascii_lowercase();
                         let fallback_exit_only =
                             fallback_exit_only_condition_ids.contains(condition_key_lc.as_str());
@@ -18960,14 +18978,13 @@ async fn main() -> Result<()> {
                                 continue;
                             }
                             let market_quote_size_mult =
-                                if ext_top_bid_usd < mm_sport_cfg_for_loop.min_top_depth_usd {
+                                if ext_top_bid_usd < market_sizing.min_top_depth_usd {
                                     MM_SPORT_LOW_DEPTH_QUOTE_SIZE_MULT
                                 } else {
-                                    mm_sport_cfg_for_loop.quote_size_mult
+                                    market_sizing.quote_size_mult
                                 };
-                            let ratio_limit =
-                                mm_sport_cfg_for_loop.max_share_ratio.clamp(0.01, 0.99);
-                            let target_bid_shares = match mm_sport_cfg_for_loop.quote_size_mode {
+                            let ratio_limit = market_sizing.max_share_ratio.clamp(0.01, 0.99);
+                            let target_bid_shares = match market_sizing.quote_size_mode {
                                 mm::MmSportQuoteSizeMode::Multiple => {
                                     (market.reward_min_size_shares * market_quote_size_mult)
                                         .max(1.0)
@@ -19054,7 +19071,7 @@ async fn main() -> Result<()> {
                             };
                             let condition_ratio_blocked =
                                 ratio_blocked_conditions.contains(&market.condition_id);
-                            let mut market_quote_size_mult = mm_sport_cfg_for_loop.quote_size_mult;
+                            let mut market_quote_size_mult = market_sizing.quote_size_mult;
                             let (up_buy_rows, down_buy_rows) = mm_sport_collect_pair_buy_rows(
                                 market_rows.as_slice(),
                                 market.up_token_id.as_str(),
@@ -19279,7 +19296,7 @@ async fn main() -> Result<()> {
                                 }
                                 continue;
                             }
-                            if pair_min_top_depth_usd < mm_sport_cfg_for_loop.min_top_depth_usd {
+                            if pair_min_top_depth_usd < market_sizing.min_top_depth_usd {
                                 market_quote_size_mult = MM_SPORT_LOW_DEPTH_QUOTE_SIZE_MULT;
                             }
 
@@ -19290,7 +19307,7 @@ async fn main() -> Result<()> {
                             let baseline_shares = (market.reward_min_size_shares
                                 * mm_sport_cfg_for_loop.pair_baseline_quote_size_mult)
                                 .max(1.0);
-                            let ratio = mm_sport_cfg_for_loop.max_share_ratio.clamp(0.01, 0.99);
+                            let ratio = market_sizing.max_share_ratio.clamp(0.01, 0.99);
                             let required_ext_top_shares =
                                 (baseline_shares * (1.0 - ratio) / ratio).max(0.0);
                             let pair_ratio_feasible = up_ext_top_bid_shares + 1e-9
@@ -19333,14 +19350,13 @@ async fn main() -> Result<()> {
                                 continue;
                             }
 
-                            let ratio_limit =
-                                mm_sport_cfg_for_loop.max_share_ratio.clamp(0.01, 0.99);
+                            let ratio_limit = market_sizing.max_share_ratio.clamp(0.01, 0.99);
                             let mut up_target_ratio = None;
                             let mut down_target_ratio = None;
                             let mut up_raw_quote_shares = None;
                             let mut down_raw_quote_shares = None;
                             let (quote_floor_shares, mut up_quote_shares, mut down_quote_shares) =
-                                match mm_sport_cfg_for_loop.quote_size_mode {
+                                match market_sizing.quote_size_mode {
                                     mm::MmSportQuoteSizeMode::Multiple => {
                                         let floor_shares = (market.reward_min_size_shares
                                             * market_quote_size_mult)
@@ -19442,7 +19458,7 @@ async fn main() -> Result<()> {
                                     json!({
                                         "strategy_id": STRATEGY_ID_MM_SPORT_V1,
                                         "condition_id": market.condition_id,
-                                        "quote_size_mode": mm_sport_cfg_for_loop.quote_size_mode.as_str(),
+                                        "quote_size_mode": market_sizing.quote_size_mode.as_str(),
                                         "quote_floor_shares": quote_floor_shares,
                                         "up_quote_shares": up_quote_shares,
                                         "down_quote_shares": down_quote_shares,
@@ -19542,7 +19558,7 @@ async fn main() -> Result<()> {
                                         json!({
                                             "strategy_id": STRATEGY_ID_MM_SPORT_V1,
                                             "condition_id": market.condition_id,
-                                            "quote_size_mode": mm_sport_cfg_for_loop.quote_size_mode.as_str(),
+                                            "quote_size_mode": market_sizing.quote_size_mode.as_str(),
                                             "pair_budget_usd": pair_budget_usd,
                                             "raw_pair_notional_usd": raw_pair_notional_usd,
                                             "raw_up_notional_usd": raw_up_notional_usd,
@@ -19570,8 +19586,7 @@ async fn main() -> Result<()> {
                                 / down_best_bid.max(0.000_001))
                             .max(1.0)
                             .max(reward_min_shares);
-                            if mm_sport_cfg_for_loop.quote_size_mode
-                                == mm::MmSportQuoteSizeMode::DepthRatio
+                            if market_sizing.quote_size_mode == mm::MmSportQuoteSizeMode::DepthRatio
                                 && (up_quote_shares + 1e-9 < up_min_quote_shares
                                     || down_quote_shares + 1e-9 < down_min_quote_shares)
                             {
@@ -20401,7 +20416,7 @@ async fn main() -> Result<()> {
                                         "strategy_id": STRATEGY_ID_MM_SPORT_V1,
                                         "condition_id": market.condition_id,
                                         "token_id": token_id,
-                                        "quote_size_mode": mm_sport_cfg_for_loop.quote_size_mode.as_str(),
+                                        "quote_size_mode": market_sizing.quote_size_mode.as_str(),
                                         "best_bid": best_bid,
                                         "raw_planned_bid_shares": raw_planned_bid_shares,
                                         "target_bid_shares": target_bid_shares,
@@ -20492,8 +20507,7 @@ async fn main() -> Result<()> {
                                 continue;
                             }
 
-                            let ratio_limit =
-                                mm_sport_cfg_for_loop.max_share_ratio.clamp(0.01, 0.99);
+                            let ratio_limit = market_sizing.max_share_ratio.clamp(0.01, 0.99);
                             let Some(observed_ratio) =
                                 mm_sport_target_share_ratio(target_bid_shares, ext_top_bid_shares)
                             else {
diff --git a/src/mm/mod.rs b/src/mm/mod.rs
index 30d012d..1fa90ba 100644
--- a/src/mm/mod.rs
+++ b/src/mm/mod.rs
@@ -89,6 +89,16 @@ impl MmSportExitMode {
     }
 }
 
+#[derive(Debug, Clone, Copy)]
+pub struct MmSportSizingProfile {
+    pub quote_size_mode: MmSportQuoteSizeMode,
+    pub quote_size_mult: f64,
+    pub multiple_collateral_cap_mult: f64,
+    pub depth_ratio_collateral_cap_mult: f64,
+    pub max_share_ratio: f64,
+    pub min_top_depth_usd: f64,
+}
+
 #[derive(Debug, Clone)]
 pub struct MmSportConfig {
     pub enable: bool,
@@ -103,11 +113,15 @@ pub struct MmSportConfig {
     pub min_reward_rate_per_day: f64,
     pub discovery_route: MmSportDiscoveryRoute,
     pub quote_size_mode: MmSportQuoteSizeMode,
+    pub nonsport_quote_size_mode: MmSportQuoteSizeMode,
     pub exit_mode: MmSportExitMode,
     pub quote_size_mult: f64,
+    pub nonsport_quote_size_mult: f64,
     pub pair_baseline_quote_size_mult: f64,
     pub multiple_collateral_cap_mult: f64,
+    pub nonsport_multiple_collateral_cap_mult: f64,
     pub depth_ratio_collateral_cap_mult: f64,
+    pub nonsport_depth_ratio_collateral_cap_mult: f64,
     pub market_allowlist_keywords: Vec<String>,
     pub market_blacklist_keywords: Vec<String>,
     pub allowed_sport_league_codes: Vec<String>,
@@ -115,7 +129,9 @@ pub struct MmSportConfig {
     pub blocked_competition_levels: Vec<String>,
     pub reward_min_shares_cap: f64,
     pub max_share_ratio: f64,
+    pub nonsport_max_share_ratio: f64,
     pub min_top_depth_usd: f64,
+    pub nonsport_min_top_depth_usd: f64,
     pub nonsport_end_exit_start_sec: u64,
     pub min_entry_top_bid_price: f64,
     pub allow_sponsored_rewards: bool,
@@ -160,6 +176,23 @@ impl MmSportConfig {
             env_u64("EVPOLY_MM_SPORT_QUOTE_EXPIRY_MIN_SEC", 65).clamp(61, 3_600);
         let quote_expiry_max_sec =
             env_u64("EVPOLY_MM_SPORT_QUOTE_EXPIRY_MAX_SEC", 185).clamp(quote_expiry_min_sec, 7_200);
+        let quote_size_mode =
+            MmSportQuoteSizeMode::from_env(std::env::var("EVPOLY_MM_SPORT_QUOTE_SIZE_MODE").ok());
+        let quote_size_mult = env_f64("EVPOLY_MM_SPORT_QUOTE_SIZE_MULT", 1.2).clamp(0.1, 20.0);
+        let multiple_collateral_cap_mult = env_f64_with_alias(
+            "EVPOLY_MM_SPORT_MULTIPLE_COLLATERAL_CAP_MULT",
+            "EVPOLY_MM_SPORT_MULTIPLE_USDC_CAP_MULT",
+            0.45,
+        )
+        .clamp(0.0, 1.0);
+        let depth_ratio_collateral_cap_mult = env_f64_with_alias(
+            "EVPOLY_MM_SPORT_DEPTH_RATIO_COLLATERAL_CAP_MULT",
+            "EVPOLY_MM_SPORT_DEPTH_RATIO_USDC_CAP_MULT",
+            0.90,
+        )
+        .clamp(0.0, 1.0);
+        let max_share_ratio = env_f64("EVPOLY_MM_SPORT_MAX_SHARE_RATIO", 0.05).clamp(0.01, 0.99);
+        let min_top_depth_usd = env_f64("EVPOLY_MM_SPORT_MIN_TOP_DEPTH_USD", 1_100.0).max(0.0);
         Self {
             enable: env_bool("EVPOLY_STRATEGY_MM_SPORT_ENABLE", false),
             hard_disable: env_bool("EVPOLY_MM_SPORT_HARD_DISABLE", false),
@@ -176,26 +209,34 @@ impl MmSportConfig {
             discovery_route: MmSportDiscoveryRoute::from_env(
                 std::env::var("EVPOLY_MM_SPORT_DISCOVERY_ROUTE").ok(),
             ),
-            quote_size_mode: MmSportQuoteSizeMode::from_env(
-                std::env::var("EVPOLY_MM_SPORT_QUOTE_SIZE_MODE").ok(),
+            quote_size_mode,
+            nonsport_quote_size_mode: MmSportQuoteSizeMode::from_env(
+                std::env::var("EVPOLY_MM_SPORT_NONSPORT_QUOTE_SIZE_MODE")
+                    .ok()
+                    .or_else(|| Some(quote_size_mode.as_str().to_string())),
             ),
             exit_mode: MmSportExitMode::from_env(std::env::var("EVPOLY_MM_SPORT_EXIT_MODE").ok()),
-            quote_size_mult: env_f64("EVPOLY_MM_SPORT_QUOTE_SIZE_MULT", 1.2).clamp(0.1, 20.0),
+            quote_size_mult,
+            nonsport_quote_size_mult: env_f64(
+                "EVPOLY_MM_SPORT_NONSPORT_QUOTE_SIZE_MULT",
+                quote_size_mult,
+            )
+            .clamp(0.1, 20.0),
             pair_baseline_quote_size_mult: env_f64(
                 "EVPOLY_MM_SPORT_PAIR_BASELINE_QUOTE_SIZE_MULT",
                 1.2,
             )
             .clamp(0.1, 20.0),
-            multiple_collateral_cap_mult: env_f64_with_alias(
-                "EVPOLY_MM_SPORT_MULTIPLE_COLLATERAL_CAP_MULT",
-                "EVPOLY_MM_SPORT_MULTIPLE_USDC_CAP_MULT",
-                0.45,
+            multiple_collateral_cap_mult,
+            nonsport_multiple_collateral_cap_mult: env_f64(
+                "EVPOLY_MM_SPORT_NONSPORT_MULTIPLE_COLLATERAL_CAP_MULT",
+                multiple_collateral_cap_mult,
             )
             .clamp(0.0, 1.0),
-            depth_ratio_collateral_cap_mult: env_f64_with_alias(
-                "EVPOLY_MM_SPORT_DEPTH_RATIO_COLLATERAL_CAP_MULT",
-                "EVPOLY_MM_SPORT_DEPTH_RATIO_USDC_CAP_MULT",
-                0.90,
+            depth_ratio_collateral_cap_mult,
+            nonsport_depth_ratio_collateral_cap_mult: env_f64(
+                "EVPOLY_MM_SPORT_NONSPORT_DEPTH_RATIO_COLLATERAL_CAP_MULT",
+                depth_ratio_collateral_cap_mult,
             )
             .clamp(0.0, 1.0),
             market_allowlist_keywords: parse_string_list_env(
@@ -214,8 +255,18 @@ impl MmSportConfig {
                 "EVPOLY_MM_SPORT_BLOCKED_COMPETITION_LEVELS",
             ),
             reward_min_shares_cap: env_f64("EVPOLY_MM_SPORT_REWARD_MIN_SHARES_CAP", 0.0).max(0.0),
-            max_share_ratio: env_f64("EVPOLY_MM_SPORT_MAX_SHARE_RATIO", 0.05).clamp(0.01, 0.99),
-            min_top_depth_usd: env_f64("EVPOLY_MM_SPORT_MIN_TOP_DEPTH_USD", 1_100.0).max(0.0),
+            max_share_ratio,
+            nonsport_max_share_ratio: env_f64(
+                "EVPOLY_MM_SPORT_NONSPORT_MAX_SHARE_RATIO",
+                max_share_ratio,
+            )
+            .clamp(0.01, 0.99),
+            min_top_depth_usd,
+            nonsport_min_top_depth_usd: env_f64(
+                "EVPOLY_MM_SPORT_NONSPORT_MIN_TOP_DEPTH_USD",
+                min_top_depth_usd,
+            )
+            .max(0.0),
             nonsport_end_exit_start_sec: env_u64(
                 "EVPOLY_MM_SPORT_NONSPORT_END_EXIT_START_SEC",
                 172_800,
@@ -276,6 +327,28 @@ impl MmSportConfig {
             .clamp(30_000, 3_600_000) as i64,
         }
     }
+
+    pub fn sizing_for_market(&self, is_sports_market: bool) -> MmSportSizingProfile {
+        if is_sports_market {
+            MmSportSizingProfile {
+                quote_size_mode: self.quote_size_mode,
+                quote_size_mult: self.quote_size_mult,
+                multiple_collateral_cap_mult: self.multiple_collateral_cap_mult,
+                depth_ratio_collateral_cap_mult: self.depth_ratio_collateral_cap_mult,
+                max_share_ratio: self.max_share_ratio,
+                min_top_depth_usd: self.min_top_depth_usd,
+            }
+        } else {
+            MmSportSizingProfile {
+                quote_size_mode: self.nonsport_quote_size_mode,
+                quote_size_mult: self.nonsport_quote_size_mult,
+                multiple_collateral_cap_mult: self.nonsport_multiple_collateral_cap_mult,
+                depth_ratio_collateral_cap_mult: self.nonsport_depth_ratio_collateral_cap_mult,
+                max_share_ratio: self.nonsport_max_share_ratio,
+                min_top_depth_usd: self.nonsport_min_top_depth_usd,
+            }
+        }
+    }
 }
 
 fn env_bool(key: &str, default: bool) -> bool {
@@ -571,6 +644,72 @@ mod tests {
         );
     }
 
+    #[test]
+    fn mm_sport_sizing_profile_falls_back_and_overrides_nonsport() {
+        with_mm_env(
+            &[
+                ("EVPOLY_MM_SPORT_QUOTE_SIZE_MODE", Some("multiple")),
+                ("EVPOLY_MM_SPORT_QUOTE_SIZE_MULT", Some("2.0")),
+                ("EVPOLY_MM_SPORT_MULTIPLE_COLLATERAL_CAP_MULT", Some("0.40")),
+                (
+                    "EVPOLY_MM_SPORT_DEPTH_RATIO_COLLATERAL_CAP_MULT",
+                    Some("0.80"),
+                ),
+                ("EVPOLY_MM_SPORT_MAX_SHARE_RATIO", Some("0.10")),
+                ("EVPOLY_MM_SPORT_MIN_TOP_DEPTH_USD", Some("1200")),
+            ],
+            || {
+                let cfg = MmSportConfig::from_env();
+                let sport = cfg.sizing_for_market(true);
+                let nonsport = cfg.sizing_for_market(false);
+                assert_eq!(sport.quote_size_mode, MmSportQuoteSizeMode::Multiple);
+                assert!((nonsport.quote_size_mult - sport.quote_size_mult).abs() < 1e-9);
+                assert!(
+                    (nonsport.multiple_collateral_cap_mult - sport.multiple_collateral_cap_mult)
+                        .abs()
+                        < 1e-9
+                );
+                assert!((nonsport.max_share_ratio - sport.max_share_ratio).abs() < 1e-9);
+                assert!((nonsport.min_top_depth_usd - sport.min_top_depth_usd).abs() < 1e-9);
+            },
+        );
+
+        with_mm_env(
+            &[
+                ("EVPOLY_MM_SPORT_QUOTE_SIZE_MODE", Some("multiple")),
+                ("EVPOLY_MM_SPORT_QUOTE_SIZE_MULT", Some("2.0")),
+                ("EVPOLY_MM_SPORT_MAX_SHARE_RATIO", Some("0.10")),
+                (
+                    "EVPOLY_MM_SPORT_NONSPORT_QUOTE_SIZE_MODE",
+                    Some("depth_ratio"),
+                ),
+                ("EVPOLY_MM_SPORT_NONSPORT_QUOTE_SIZE_MULT", Some("0.7")),
+                (
+                    "EVPOLY_MM_SPORT_NONSPORT_MULTIPLE_COLLATERAL_CAP_MULT",
+                    Some("0.25"),
+                ),
+                (
+                    "EVPOLY_MM_SPORT_NONSPORT_DEPTH_RATIO_COLLATERAL_CAP_MULT",
+                    Some("0.55"),
+                ),
+                ("EVPOLY_MM_SPORT_NONSPORT_MAX_SHARE_RATIO", Some("0.05")),
+                ("EVPOLY_MM_SPORT_NONSPORT_MIN_TOP_DEPTH_USD", Some("900")),
+            ],
+            || {
+                let cfg = MmSportConfig::from_env();
+                let sport = cfg.sizing_for_market(true);
+                let nonsport = cfg.sizing_for_market(false);
+                assert_eq!(sport.quote_size_mode, MmSportQuoteSizeMode::Multiple);
+                assert_eq!(nonsport.quote_size_mode, MmSportQuoteSizeMode::DepthRatio);
+                assert!((nonsport.quote_size_mult - 0.7).abs() < 1e-9);
+                assert!((nonsport.multiple_collateral_cap_mult - 0.25).abs() < 1e-9);
+                assert!((nonsport.depth_ratio_collateral_cap_mult - 0.55).abs() < 1e-9);
+                assert!((nonsport.max_share_ratio - 0.05).abs() < 1e-9);
+                assert!((nonsport.min_top_depth_usd - 900.0).abs() < 1e-9);
+            },
+        );
+    }
+
     #[test]
     fn mm_sport_config_reads_post_only_override() {
         with_mm_env(&[("EVPOLY_MM_SPORT_POST_ONLY", Some("false"))], || {
diff --git a/strategy-changelog.md b/strategy-changelog.md
index 8c8d7b7..970fbb1 100644
--- a/strategy-changelog.md
+++ b/strategy-changelog.md
@@ -10,6 +10,9 @@ Older entries may reference env keys that were removed in later commits.
 ## Change Log
 
 ### 2026-05-02
+- `mm_sport_v1` / MM 2.0 route-specific sizing profiles: added optional Non-S sizing overrides for quote mode, quote multiplier, collateral caps, max share ratio, and min visible depth (`src/mm/mod.rs`, `src/main.rs`, `.env.example`, `.env.full.example`, `docs/mm_sport_v1.md`).
+  - Sport markets keep using existing `EVPOLY_MM_SPORT_*` sizing keys; Non-S markets use `EVPOLY_MM_SPORT_NONSPORT_*` values when present and otherwise fall back to Sport values.
+  - Applies only to fresh BUY entry sizing and BUY-side depth/ratio hygiene, including FIFO/ratio checks. SELL exits, inventory cleanup, end-date halt, top-bid floor, sponsored filtering, and WS scope plumbing remain on existing logic.
 - `mm_sport_v1` / MM 2.0 WS FIFO subscription pressure guard: scope refreshes now keep a sticky in-process superset of subscribed assets/markets, merge mixed add/remove target changes into the active superset before reconnecting, defer shrink-only changes, and explicitly drop old WS streams before reconnecting (`src/polymarket_ws.rs`).
   - affects MM 2.0 Sport, Non-S, and Dual FIFO visibility; new open-order markets still trigger prompt subscription, while normal quote-loop scope shrink/toggle noise no longer creates repeated Polymarket WS connections.
 - `mm_sport_v1` / MM 2.0 desktop SaaS parity fixes: hardened the Non-S/Dual guard backport so top-bid entry checks use external orderbook bids after removing our own BUY depth, one-sided `NoExit` entries honor the same top-bid floor, sponsored-reward exclusion fails closed when its source endpoint is unavailable, and WS scope debounce no longer sleeps inside the stream select loop (`src/main.rs`, `src/polymarket_ws.rs`).
